#In this task, we will decide ourselves what value to use as a separator in the
#sep parameter.
#The program takes a string as input, which will be used as a separator, and your task is to print the
#numbers from 1 to 5 , with the entered separator between

separator = input("Enter your separator: ")
print(1, 2, 3, 4, 5, sep=separator)